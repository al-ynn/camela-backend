info:
  name: Update Profile
  type: http

http:
  method: PUT
  url: http://127.0.0.1:8000/api/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
