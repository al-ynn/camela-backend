info:
  name: Order Single
  type: http

http:
  method: GET
  url: http://127.0.0.1:8000/api/admin/orders/{id}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
