info:
  name: Update
  type: http

http:
  method: PATCH
  url: http://127.0.0.1:8000/api/cart/{id}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
