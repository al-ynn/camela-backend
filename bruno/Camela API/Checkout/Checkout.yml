info:
  name: Checkout
  type: http

http:
  method: POST
  url: http://127.0.0.1:8000/api/checkout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
