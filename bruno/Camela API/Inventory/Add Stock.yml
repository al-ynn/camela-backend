info:
  name: Add Stock
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/api/admin/products/1/inventory
  body:
    type: json
    data: |-
      {
          "type":"IN",
          "quantity":50,
          "remarks":"Restock"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
