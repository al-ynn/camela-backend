info:
  name: "Create Pay "
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/payments/create
  body:
    type: json
    data: |-
      {
          "payment_url":"..."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
